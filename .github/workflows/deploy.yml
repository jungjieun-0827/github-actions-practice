name: Github Actions Execution

on:
  push:
    branches: 
        - main

jobs:
    My-Deploy-Job:
        runs-on: ubuntu-latest
        steps:
            - name: Hello World Action
              run: echo "Hello, World!"
